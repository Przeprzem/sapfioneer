export enum cssProperties {
    backgroundColor = 'background-color'
}

export enum contactValidationMessages {
    invalidEmail = 'Email must be formatted correctly.',
    invalidPhone = 'A valid phone number may only contain numbers, +()-. or x'
}